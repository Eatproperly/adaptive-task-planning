---
name: adaptive-task-planning
description: 用户说“使用任务拆解”时触发本技能，也可通过 $adaptive-task-planning 调用。与用户逐步对齐学习、个人项目及其他复杂任务，将目标拆成适合用户执行的清晰任务树，管理依赖、投入标准、关键前提及动态重规划。用于大任务拆解、计划可行性梳理、执行受阻后的计划修订；不用于直接代替用户执行任务或简单的一步请求。
---

# 自适应任务拆解

## 中文触发语

用户发出“使用任务拆解”的指令时，使用本技能进入规划流程；仅引用或讨论这句话不视为调用。若同条消息包含任务，直接结合已有上下文开始；若未提供任务且上下文无法确定，先问“你想拆解什么任务？”不要要求用户记住英文技能名。触发后仍须先对齐重要性、期待和关键约束，再开始拆解；不自动执行计划中的任务。

帮助用户得到能理解、能开始、能检查、能随现实变化修订的计划。默认由用户执行，是否使用 AI 由用户决定。规划请求不授权执行计划中的工作、自动委派、发送消息或安排后台监控。

## 1. 拆解前先理解任务

结合已有信息分析任务性质，针对缺口与用户对齐，不从零重复访谈。先明确：

- 目的与价值：真正要获得什么；重要性由用户说明，不替用户评判什么值得认真做。
- 期待：最低可接受结果、理想结果、可以放弃的部分；硬性要求单列。
- 边界：范围、非目标、截止时间、结束条件；开放目标先收敛成一个可判断的阶段目标。
- 可行性与资源：可投入的时间、精力、预算、工具、外部依赖，以及用户的相关经验。
- 已知与未知：区分有依据的事实、暂定假设、约束和偏好；指出冲突及不可行之处。

重要性与期待尚未明确时，必须向用户询问；已有答案可直接复用。AI 可提出后果与取舍，不擅自降低目标。必要时建议直接做、缩小、暂缓或放弃，由用户决定。

提问以一个当前最影响决策的问题为单位，并说明答案会改变什么。不要倾倒问卷，也不要对可合理推断的细枝末节反复确认。

**拆解门槛：** 能简短复述目标、范围、验收标准、投入上限和关键约束，给用户纠正机会；没有会改变首轮拆解的未解决歧义。缺失的重要信息要先问清楚。执行后才能知道的结果，不要求提前猜定：登记为假设，安排有明确结束条件的验证任务。只有验证目标已清楚，才拆这个验证任务。

## 2. 选择投入标准

按用户的重视程度与期待建议模式；存在实质取舍时与用户对齐。同一计划允许不同分支采用不同标准。

| 模式 | 目标 | 规划与检查 |
| --- | --- | --- |
| 高保障 | 达到约定的较高质量 | 重点细化关键步骤，设置证据检查、返工条件和关键前提预案 |
| 够用 | 以合理的最小投入达到最低可接受结果 | 保留必需步骤，删减选做、美化和低收益优化，达标即停止 |

“80%—90%”或“60%”表达投入倾向，不是通用完成率或任意评分。将它们转成具体交付与验收条件；有真实评分规则时依据该规则。截止时间、必交内容等硬性底线不能随模式降低。重要性决定保障力度；熟悉程度和操作复杂度决定拆解粒度。两种模式下，可执行任务都必须清晰。

## 3. 建立任务树和关联

先建立当前理解下的全局结构，再逐步细化近期任务。结构是可修订的，不声称提前穷尽未知工作。

- 使用稳定任务编号表达父子关系，例如 T1、T1.1；父任务说明其成果和与总目标的关系。
- 单独记录依赖：前置任务及其具体输出、什么条件满足才解锁。不能只写“依赖 A”而不说明依赖什么。
- 区分完成依赖、共享资源约束、影响关系和备选路线。备选路线不是全部必做。
- 执行依赖应无循环；反馈迭代写成有停止条件的一轮轮任务，避免互相等待。
- 对关键分支说明：A 产生哪些结果，会使 B/C 解锁、修改、取消或重新验证。影响不确定时写明检查点，不编造结论。
- 从最终验收标准反查任务覆盖，再从任务反查对目标的贡献，查漏与删减无关工作。

近期任务细化至可执行；依赖未知结果的远期节点标为“待展开”，保留目的、预期成果和展开触发条件，不伪造细节。优先解决会阻塞多个分支的不确定性。

## 4. 与用户校准何时停止拆分

先从相关经验校准，不用笼统的新手/高手标签。必要时问“类似的事情，你最近独立完成过什么？”不将某领域熟练程度套到其他领域。

选择一个近期代表性任务，给出动作、输入、产出和验收，然后问：“假设现在开始，你知道第一步怎么做吗？哪里还需要猜或先查清楚？”

| 用户反馈 | 处理 |
| --- | --- |
| 知道怎么开始，标准清楚，负担合适 | 停止拆分 |
| 不懂术语或要求 | 解释或改写，不机械增加子任务 |
| 理解目标但不会下手 | 只细化卡住的部分 |
| 缺材料、工具或前置能力 | 增加准备或学习任务，关联后续依赖 |
| 方法是否有效未知 | 安排最小验证，明确时限、产出、成功/失败/证据不足的后续处理 |
| 会做但工作量过大 | 按可独立检查的中间成果拆分 |

**可执行门槛，缺一不可：**

1. 动作、对象和范围具体，不以“优化一下、深入学习、完善方案”代替要求。
2. 输入与前置条件明确、可获得；未满足的依赖使任务保持等待。
3. 产出或可观察的状态变化明确，验收方法与通过条件清楚。
4. 无隐藏的关键决策；关键阻碍有验证、求助或替代处理办法。
5. 用户已通过明确反馈、相关执行经历或同类任务校准，表明知道如何开始；任务负担符合约定安排。

新领域或关键任务可请用户简述第一步来检查理解，不进行重复考试。同类任务沿用已校准粒度；情境明显变化则重新校准。未知时标“待校准”，不声称适合用户执行。用户可随时要求再细化或合并。

探索、创作任务约定本轮动作、成果及停止条件，允许结果未知。学习任务按目标采用回忆、解释或应用检查；除非目标只是浏览，否则看完材料不等于学会。

## 5. 按一个人的实际资源排序

区分逻辑独立和实际可并行：用户只有一份时间与精力。根据截止时间、依赖、精力要求和可用时段排序，限制同时推进数量；没有偏好时建议一次聚焦一项主动工作，等待期间再切换。

工时使用有依据的范围并标注不确定性，给关键任务留缓冲。若无法在约束内达标，明确冲突并与用户讨论范围、期限或资源。不要用不真实的工期使计划看起来可行。

## 6. 管理前提与预案

首次建立关键前提预案，或收到前提失效报告时，读取 [references/premise-recovery.md](references/premise-recovery.md)。只管理能改变计划的关键前提，避免穷举低影响风险。

原则：可恢复且值得恢复的现实条件，优先修复；被证据否定的认知假设，先纠正。为修复设成功标准、投入上限和退出条件。无法合理修复时，重新对齐需求与条件，制定新计划；不假定用户目标必然改变，也不擅自改走替代路线。

## 7. 交付、反馈和重规划

交付计划或维护持久记录时读取 [references/plan-format.md](references/plan-format.md)，按任务规模裁剪。默认可在对话中完成，不强制建文档；需要跨会话续接或用户要求保存时提供计划文件。

规划停止条件：理解门槛通过，全局结构与关键关系可解释，近期任务通过可执行门槛，关键前提有处理方式，且下一步明确。不要等待所有远期分支展开，也不要无限访谈。

依据用户报告的结果、证据、障碍和条件变化更新计划，不假装看到了执行过程，不自动监控。不要求每个普通任务提交证明，但区分用户报告、可检查证据与 AI 推断，关键下游依赖尤其要检查结果是否可用。

每次关键反馈：

1. 核对具体任务和验收标准，记录实际产出；“做过”不自动等于通过。
2. 更新状态，沿依赖与影响关系检查受影响分支；列出解锁、修改、取消、重新验证的理由。
3. 在关键里程碑检查局部成果是否推进总目标；全部打勾不代替整体验收。
4. 原目标和前提仍有效时局部调整；关键前提无法恢复时启动重新对齐和新计划。
5. 保留稳定编号、已完成成果、旧版本及变化原因；改变目标、质量、范围或投入上限需与用户对齐。
6. 给出用户现在能做的下一项任务，以及下一次需要反馈的结果或触发条件。

已停止拆分的任务也可重新打开：用户发现不清楚时只细化卡点，保留已完成部分并复核依赖。修订计划文件前保留可恢复版本或使用已有版本控制，不覆盖用户修改。

## 最终质量检查

- 重要性和期待来自用户；质量目标已具体化，硬性底线保留。
- 进入执行队列的任务同时通过客观清晰度与用户可执行性检查。
- 每项终点标准有任务覆盖，依赖不循环，备选路线没有被当成全部必做。
- 资源安排适合用户，未知结果没有被包装为事实，远期待展开项有触发条件。
- 关键前提关联了受影响任务，有修复/纠错判断及退出条件。
- 当前版本、变化原因与下一步清楚；记录和提问确实帮助决策或行动。

承诺检查与纠偏流程，不承诺任务永远无歧义或计划一定成功。不要为维护完整外观捏造材料、工具可用性、进度或条件。
